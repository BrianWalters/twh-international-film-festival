<dl>
    <dt>Director</dt>
    <dd>{{ movie.secret ? "SECRET" : omdb(movie).Director }}</dd>
    <dt>Actors</dt>
    <dd>{{ movie.secret ? "SECRET" : omdb(movie).Actors }}</dd>
    <dt>Writer</dt>
    <dd>{{ movie.secret ? "SECRET" : omdb(movie).Writer }}</dd>
</dl>