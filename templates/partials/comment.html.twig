<div class="card" style="margin-bottom: 24px;">
    <div class="card-header">
        By <strong>{{ comment.commenter }}</strong>
        {{ comment.timeRelativeToMovie(movie) }}
    </div>
    <div class="card-body">
        <p>
            {{ comment.text }}
        </p>
    </div>
</div>